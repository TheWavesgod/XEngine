#pragma once

namespace XEngine
{
    class VulkanBuffer
    {
    public:
        VulkanBuffer() = default;
        ~VulkanBuffer() = default;

        // TODO(Stage 2B): Own VkBuffer and VMA allocation handles.
    };
}
