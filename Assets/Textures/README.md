Place simple development textures here for Stage 6B testing.

Suggested file:
- checker.png

Stage 6B will try to load Assets/Textures/checker.png if present.
If it is missing, the engine falls back to default textures.
